'use strict';

let x = Number(prompt("Введите x = "));
let y = Number(prompt("Введите y = "));

let z = (x - y)/ Math.sqrt(x = y) + x * Math.pow(y, 2)/Math.sin(Math.pow(x, 2)) * Math.pow(Math.cos(y), 2);

console.log(z);
